[pytest]
base_url = https://www.saucedemo.com/

pythonpath = .

addopts =
    --browser=chromium
    --headed
    --slowmo=1000
    ; --tracing=retain-on-failure
    --screenshot=on
    ; --video=retain-on-failure
    --html=report.html
    --self-contained-html

